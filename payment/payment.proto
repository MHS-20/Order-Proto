syntax = "proto3";
option go_package = "github.com/MHS-20/Order-Proto/payment";

message CreatePaymentRequest {
	float price = 1;
}

message CreatePaymentResponse {
	int64 bill_id = 1;
}

service PaymentService {
    rpc Create(CreatePaymentRequest) returns (CreatePaymentResponse) {}
}